package com.luv2code.springdemo;

public class MyApp {

	public static void main(String[] args) {
		//create the object
		//Coach theCoach = new BasketballCoach();
		//System.out.print(theCoach.getDailyWorkout());
		
		//Spring gives fully assembled objects 
		
		// Flow of the learning
		/*
		 * 1. Introduction to Spring and development 
		 * 2. Spring inversion of control --> we are creating the class and invoking the object using beans(configuration file)
		 * 3. Dependency Injection --> Adding the dependency for all the class through configuration file either by using constructor or setter dependency
		 * 4. Constructor Dependency --> We are invoking the dependency interface using constructor and constructor-arg tag
		 * 5. Setter Dependency --> We are invoking the dependency interface using setter function and property name tag
		 * 6. Setter for setting the value of literals
		 * 7. Setting the values from the properties files <IMPORTANT> Always write the classpath from package
		 * 8. Bean Scopes --> Singleton and Prototype
		 * 9. Bean Life cycle
		 */
		
	}

}
